import React from 'react';
import s from './Footer.module.css';

const Footer = () => {
    return <div className={s.footer}>
        <p className={s.footerItem}>Social Network</p>
    </div>
};

export default Footer;