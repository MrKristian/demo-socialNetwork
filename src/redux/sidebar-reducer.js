
let initialState = {
    friends: [
        { id: 1, name: 'Ostap' },
        { id: 2, name: 'Bodya' },
        { id: 3, name: 'Dima' }
    ]
};

const sidebarRecurser = (state = initialState, action) => {

    return state;
};

export default sidebarRecurser;